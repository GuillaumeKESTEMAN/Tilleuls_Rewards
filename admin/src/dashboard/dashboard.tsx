import * as React from "react";
import { Card, CardHeader } from '@mui/material';

const Dashboard = () => (
    <Card>
        <CardHeader title="Bienvenue dans le dashboad admin" />
    </Card>
);

export default Dashboard;
